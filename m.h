#include<windows.h>
#include<list>
#include"resource.h"
#include<time.h>

using namespace std;


#define BACK_MOVE_TIMER_ID			(WM_USER+1)  
#define SENG_GUNNER_TIMER_ID		(WM_USER+2)
#define GUNNER_MOVE_TIMER_ID		(WM_USER+3)
#define CREATR_FOEPLANE_TIMER_ID	(WM_USER+4)
#define FOEPLANE_MOVE_TIMER_ID		(WM_USER+5)
#define BLAST_SHOWID_TIMER_ID		(WM_USER+6)